import { EasyModalDirectiveDirective } from './easy-modal-directive.directive';

describe('EasyModalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EasyModalDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
